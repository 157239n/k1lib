To generate new docs, run the script "./compile.py". The directory structure should look like this:

```
~/
    repos/
        labs/
            k1lib/
                docs/
            k1lib.github,io/
```
